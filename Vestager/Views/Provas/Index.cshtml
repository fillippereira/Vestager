@model IEnumerable<Vestager.Domain.Entities.Prova>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataProva)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.UserID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataProva)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProvaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProvaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProvaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
